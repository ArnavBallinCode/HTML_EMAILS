# HTML Email Sender

A simple and elegant Python script to send beautifully designed HTML emails with embedded inline images.

## Setup and Usage

1. Install dependencies: `pip install python-dotenv`
2. Add your credentials (email and App Password) to a `.env` file.
3. Run the script: `python3 send_email.py`
